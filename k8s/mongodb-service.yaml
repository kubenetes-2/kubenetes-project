apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: dev-db
  labels:
    app: mongodb
spec:
  type: ClusterIP
  clusterIP: 10.233.18.154  # ClusterIP 명시적으로 고정 (안정적)
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb

